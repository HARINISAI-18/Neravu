# ---- API layer ----
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9        # REQUIRED for /api/stt file upload — easy to miss
python-dotenv>=1.0

# ---- RAG stack (your existing) ----
numpy>=1.24,<2.0               # pin <2 for older faiss/torch wheels; loosen if fresh install works
faiss-cpu>=1.7.4               # use faiss-gpu only if CUDA toolkit installed
torch>=2.1                     # GPU: install CUDA build from pytorch.org, not this line
sentence-transformers>=2.6
transformers>=4.40
accelerate>=0.29               # needed for device_map / quantization
rank-bm25>=0.2.2
pypdf>=4.0

# ---- scheme matching (schemes.xlsx) ----
pandas>=2.0
openpyxl>=3.1

# ---- multilingual + voice (new) ----
edge-tts>=6.1
faster-whisper>=1.0
deep-translator>=1.11

# ---- optional: Gemini backend (GEN_BACKEND=gemini) ----
google-generativeai>=0.5

# ---- optional: 4-bit GPU loading of Qwen ----
# bitsandbytes>=0.43
# torch — install the correct build separately first:
#   GPU:  pip install torch --index-url https://download.pytorch.org/whl/cu121
#   CPU:  pip install torch --index-url https://download.pytorch.org/whl/cpu